program test(input bit [3:0] addr);
initial begin
    $display("the value of the address is: %0d",addr);
end
endprogram
//program samples data in the reactive region, after all design statments get exectued in this time unit
